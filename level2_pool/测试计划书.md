# 二级 IP 池（子 IP 池 / 站点级）测试计划书

## 1. 测试目标与范围

验证二级池**增量同步（含空响应 max_id 判定）、站点连通测试、租赁分配（含提取策略/延迟与剩余时间筛选/批量提取）/释放/删除、周期复验、TTL 淘汰与对外 API** 的正确性、并发安全性与鲁棒性。租赁语义与并发分配是测试重点。

**范围**：`app/` 分层结构下 `sync/`（client/syncer）、`core/`（pool/stats）、`testing/tester.py`、`tasks/`（revalidate/ttl）、`api/`（routes/params/serializers）、`launcher.py` 与 `main.py` 装配。
**不在范围**：供应商拉取（一级池）、代理层路由。

## 2. 测试环境与工具

| 项 | 说明 |
|---|---|
| Python | 3.14 |
| 测试框架 | pytest + pytest-asyncio |
| 网络桩 | aioresponses（模拟一级池）+ 本地 mock 一级池服务 |
| 覆盖率 | pytest-cov |
| 依赖文件 | `requirements-dev.txt`：`pytest, pytest-asyncio, pytest-cov, aioresponses` |

## 3. 测试层级

| 层级 | 说明 |
|---|---|
| 单元测试 | pool/syncer/tester/tasks 纯逻辑，网络打桩 |
| 集成测试 | mock 一级池 → 同步 → 站点测试 → 入池 → acquire 闭环 |
| 端到端 | 一级池 + 二级池 + mock 供应商跨服务联调 |
| 性能测试 | 百并发 acquire、同步吞吐 |
| 鲁棒性测试 | 一级池重启/不可达、空池、高并发竞态 |

## 4. 测试用例

### 4.1 core/pool.py（核心，含提取策略注册表）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L2-POOL-001 | `upsert` 按 `proxy_url` 去重 | 已有同身份记录 | 再次 upsert | 刷新延迟/ttl/时间，不新增记录 | P0 |
| L2-POOL-002 | 本地 id 自增不复用 | 多次 upsert 新身份 | 观察 id | 唯一且单调递增 | P0 |
| L2-POOL-003 | `acquire` 最新优先 | 多条空闲 | acquire | 返回最新入池的空闲记录 | P0 |
| L2-POOL-004 | `acquire` 跳过已租赁 | 最新已租、次新空闲 | acquire | 返回次新空闲 | P0 |
| L2-POOL-005 | 租赁无过期 | 已租记录 | 等待+再次 acquire | 不自动释放 | P0 |
| L2-POOL-006 | `acquire` 原子性（100 并发） | 仅 1 空闲 | 100 协程 acquire | 仅 1 成功，其余返回空池/明确错误 | P0 |
| L2-POOL-007 | `release(id)` | 已租记录 | release | 解除标记，可再次 acquire | P0 |
| L2-POOL-008 | `remove(id)` | 记录 | remove | 记录删除（含解除租赁） | P0 |
| L2-POOL-009 | `release_all` | 多条已租 | release_all | 全部解除，返回数量 | P0 |
| L2-POOL-010 | `release/remove` 不存在的 id | 无此 id | 操作 | 返回 False，不抛异常 | P1 |
| L2-POOL-011 | `stats()` 细分 | 混合租/闲/协议 | 统计 | 池内/租赁/空闲 按协议数精确 | P0 |
| L2-POOL-012 | `all()` 不产生租赁标记 | 查询 | 多次 all | 记录 leased 状态不变 | P0 |
| L2-POOL-013 | TTL 淘汰 | 含过期项 | `sweep_ttl` | 仅删过期 | P1 |
| L2-POOL-014 | `acquire` 显式 `latest` 与默认一致 | 多条空闲 | acquire | 与旧行为一致：最新入池优先（向后兼容） | P0 |
| L2-POOL-015 | `acquire` 策略 `latency_asc` | 延迟错落 | acquire | 直接取延迟最低一条（不排序 argmin），并列取先入池 | P0 |
| L2-POOL-016 | `acquire` 策略 `remaining_desc` | 剩余时间错落 | acquire | 直接取剩余最长一条（argmax），并列取先入池 | P0 |
| L2-POOL-017 | `remaining_desc` 中 `ttl=None` 视为无穷大 | ttl=None 与长 ttl 并存 | acquire | ttl=None 最优先 | P0 |
| L2-POOL-018 | `acquire` 策略 `random` 唯一性 | 5 条空闲 | 连续取 5 次 | 不重复、取尽后返回 None | P0 |
| L2-POOL-019 | 筛选 `max_latency_ms` | 延迟混合 | acquire | 仅延迟 ≤ 上限的空闲可被选中 | P0 |
| L2-POOL-020 | 筛选 `min_remaining_sec` | 剩余时间混合 | acquire | 仅剩余 ≥ 下限可选；已过期未清扫被 0 排除；ttl=None 恒通过 | P0 |
| L2-POOL-021 | 筛选后无候选 | 全部超标 | acquire | 返回 None，不误标租赁 | P0 |
| L2-POOL-022 | `acquire_batch` 策略 `latest` | 多条空闲 | 批量提取 | 最新在前，标记租赁 | P0 |
| L2-POOL-023 | `acquire_batch` 按策略排序 | 延迟/剩余错落 | 批量提取 | `latency_asc` 升序 / `remaining_desc` 降序（并列先入池在前） | P0 |
| L2-POOL-024 | `acquire_batch` 策略 `random` 去重 | count ≥ 池容量 | 批量提取 | 返回全池且无重复 | P0 |
| L2-POOL-025 | `acquire_batch` 部分满足 | count > 空闲数 | 批量提取 | 返回能租到的全部；无候选返回 `[]` | P0 |
| L2-POOL-026 | `acquire_batch` 并发原子性 | 容量 3 | 100 协程各 count=2 | 无重复租赁，总计恰 3 条 | P0 |
| L2-POOL-027 | `acquire_batch` 非法 count | count≤0 | 批量提取 | 抛 ValueError（HTTP 层转 40000） | P1 |

### 4.2 sync/（含空响应 max_id 判定）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L2-SYNC-001 | 首次全量同步 | 桩 `/ips` 有记录 | 首 tick | 拉全量，`last_synced_id`=最大 id | P0 |
| L2-SYNC-002 | 增量推进水位线 | 桩 `/ips/after/{id}` 有新增 | 后续 tick | 仅取新增，水位线推进 | P0 |
| L2-SYNC-003 | 空响应且水位线 > max_id 触发全量重拉 | 桩 after 返回空、`max_id` 低于水位线 | tick | 改调 `/ips` 全量 | P0 |
| L2-SYNC-003b | 空响应且水位线 == max_id 不重拉 | 桩 after 返回空、`max_id` == 水位线 | tick | 不调 `/ips`，`total_pulled` 不变 | P0 |
| L2-SYNC-003c | 空响应且 max_id 缺失回退全量 | 桩 after 返回空、无 max_id 字段 | tick | 回退全量重拉（向后兼容） | P0 |
| L2-SYNC-004 | 空响应重置水位线 | after 空、全量有新 id | tick | `last_synced_id` 重置为全量最大 id | P0 |
| L2-SYNC-005 | 重置不移除现存记录 | 池内已有记录 | 空响应重置 | 空闲与租赁记录均保留 | P0 |
| L2-SYNC-006 | 站点测试 <2000ms 才入池 | 桩延迟混合 | 同步 | 延迟超标者不入池 | P0 |
| L2-SYNC-007 | 首次全量为空 | 桩 `/ips` 空 | 首 tick | 水位线保持 None，不报错 | P1 |
| L2-SYNC-008 | 同步异常不影响池 | 一级池不可达 | tick | 现有记录不受影响，下次重试 | P0 |
| L2-SYNC-009 | 拉取/测试解耦：慢测试不阻塞拉取节奏 | 测试慢 | 多 tick | 同步间隔不受测试耗时影响 | P0 |
| L2-SYNC-010 | 队满丢最旧批次 | buffer 满 | 入队 | 丢最旧批次、`drops`/`stats.drops` 累计 | P0 |
| L2-SYNC-011 | 单批测试异常隔离 | worker 遇异常 | 运行 | 仅该批记 `test_failures`，循环继续 | P0 |
| L2-SYNC-012 | worker 数量自动计算/上限截断 | 各 concurrency | 计算 | 默认 `max(1, concurrency//10)`，显式值超上限截断 | P1 |
| L2-SYNC-013 | 多 worker 并行且总并发受限 | 大批次 | 运行 | 并行消费、总并发 ≤ concurrency 量级 | P1 |

### 4.3 testing/tester.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L2-TEST-001 | `site_filter` 延迟阈值 | 桩延迟 <2000 / >2000 | 过滤 | 仅 <2000ms 通过；等于阈值判失败 | P0 |
| L2-TEST-002 | `revalidate` 代理可达性 | 桩可达/不可达 | 复验 | 仅返回存活项（不测出口） | P0 |
| L2-TEST-003 | 站点超时 | 桩超时 | site_filter | 判失败，不悬挂 | P1 |
| L2-TEST-004 | 批次汇总日志（含失败原因） | 混合失败原因 | site_filter | 日志含 `total=X ok=Y fail=Z` 及 `timeout*N` 等计数 | P1 |
| L2-TEST-005 | 复验汇总日志 | 混合失败 | revalidate | 同上格式 | P1 |
| L2-TEST-006 | 空输入不输出日志 | 空列表 | site_filter/revalidate | 返回空、无汇总日志 | P1 |

### 4.4 tasks/（revalidate / ttl）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L2-TASK-001 | 同步周期 3s | 假时钟 | 多 tick | 间隔 ≈ sync_interval | P1 |
| L2-TASK-002 | 复验 60s 删除不通过项 | 含失效项 | 复验 tick | 失败项被删除 | P0 |
| L2-TASK-002b | 复验删除含租赁中的失效项 | 租赁项失效 | 复验 tick | 该租赁项也被删除 | P0 |
| L2-TASK-003 | TTL 周期淘汰 | 含过期项 | 清扫 tick | 过期项删除 | P1 |

### 4.5 api/（routes + params + serializers，含 main/launcher 装配）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L2-RT-001 | `/status` 字段 | 运行态 | GET | 含 uptime/total_pulled/total_entered/api_call_count/last_synced_id/errors/drops/pool_stats | P0 |
| L2-RT-002 | `/count` 细分 | 混合租/闲/协议 | GET | total/by_proto/leased_total/leased_by_proto/free_total/free_by_proto 精确 | P0 |
| L2-RT-003 | `/ips` 全部 | 多记录 | GET | 返回全部，不产生租赁标记 | P0 |
| L2-RT-004 | `/ips/acquire` | 有空闲 | POST | 返回 1 条并标记租赁 | P0 |
| L2-RT-005 | `/ips/acquire` 空池 | 无空闲 | POST | 返回明确错误码 40402（非 500） | P0 |
| L2-RT-006 | `/ips/{id}/release` | 已租 | POST | 释放成功 | P0 |
| L2-RT-007 | `/ips/{id}` 删除 | 记录 | DELETE | 删除成功 | P0 |
| L2-RT-008 | `/ips/release-all` | 多条已租 | POST | 全部释放，返回数量 | P0 |
| L2-RT-009 | 不存在的 id 操作 | 无效 id | release/delete | 返回明确错误，非 500 | P1 |
| L2-RT-010 | api_call_count 递增 | 连续请求 | 多次 | 逐次 +1 | P1 |
| L2-RT-011 | `/ips/acquire` 策略与筛选参数生效 | 延迟/剩余错落 | POST 带参数 | 按参数选取（默认无参 = 旧行为回归） | P0 |
| L2-RT-012 | `/ips/acquire` 非法参数 40000 | strategy 未知/筛选非数字/负数 | POST | `code:40000`，不产生租赁 | P0 |
| L2-RT-013 | `/ips/acquire` 筛选后无候选 | 全部超标 | POST | `code:40402`，`empty_acquires` +1 | P0 |
| L2-RT-014 | `/ips/acquire-batch` 契约 | 空闲若干 | POST count=N | 返回数组按策略排序；不足 N 尽量多给（code 0） | P0 |
| L2-RT-015 | `/ips/acquire-batch` count 校验 | 缺失/非整数/<1 | POST | `code:40000` | P0 |
| L2-RT-016 | `/ips/acquire-batch` 空池 | 无候选 | POST | `code:40402`，`empty_acquires` 计一次 | P0 |

## 5. 集成测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| L2-INT-001 | mock 一级池 → 同步 → 站点测试 → 入池 → acquire | 闭环，acquire 返回可用 IP | P0 |
| L2-INT-002 | 一级池重启（id 归零） | 空响应触发全量重拉，服务无感恢复 | P0 |
| L2-INT-003 | acquire/release 完整流程 | 释放后可再次被获取 | P0 |
| L2-INT-004 | 同步 + 复验 + TTL 三任务并发运行 | 互不干扰、计数一致 | P1 |

## 6. 鲁棒性与性能测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| L2-ROB-001 | 一级池不可达 | 现有租赁不受影响，tick 重试 | P0 |
| L2-ROB-002 | 池空 acquire | 明确错误码，不崩溃 | P0 |
| L2-ROB-003 | 百并发 acquire | 无重复分配、无竞态 | P0 |
| L2-ROB-004 | 百并发 acquire_batch（含策略/筛选） | 无重复分配、无竞态 | P0 |
| L2-PERF-001 | 100 线程同时取 IP | 分配正确且延迟可接受 | P1 |
| L2-PERF-002 | 持续同步吞吐 | 每 3s 同步无积压 | P1 |

## 7. 测试桩与 Fixture 设计

- mock 一级池：本地 FastAPI 服务，实现 `/ips`、`/ips/after/{id}`，可配置返回记录集、id 空间重置（模拟重启）、延迟/故障。
- `conftest.py`：
  - `event_loop`、`mock_session`（aioresponses）
  - `level1_client`：指向 mock 一级池的 `Level1SyncClient`
  - `pool`：预置记录（混合协议/租闲状态）的 Level2Pool fixture
  - `tester`：可注入延迟的站点测试桩
- 桩对象：一级池全量/增量/空/异常响应；站点测试 <2000ms、>2000ms、超时。

## 8. 覆盖率目标

| 模块 | 目标 |
|---|---|
| pool / syncer / routes | ≥ 95% |
| tester / tasks | ≥ 90% |
| 整体 | ≥ 90% |

## 9. 执行方式

```bash
pip install -r requirements-dev.txt

# 单元 + 集成
pytest -v --cov=app --cov-report=term-missing

# 仅 pool（租赁核心）
pytest tests/test_pool.py -v

# 同步（空响应 max_id 判定）
pytest tests/test_syncer.py -v

# 集成
pytest tests/test_integration.py -v

# 性能
pytest tests/test_perf.py -v -m perf
```

## 10. 缺陷管理与退出准则

- **缺陷分级**：P0（阻断）必须修复复测；P1（重要）需修复；P2 可排期。
- **退出准则**：P0/P1 清零；pool/syncer/routes 覆盖率 ≥ 95%；全部用例通过后方可发布。
- 二级池改动需回归与一级池、代理层的跨项目契约（`/ips/after/{id}` 空响应 + `max_id` 语义、透传端点）。
