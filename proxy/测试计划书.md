# 代理层（接口 IP 池）测试计划书

## 1. 测试目标与范围

验证代理层**路由表加载与热更新、按站点透传分发、健康检查**的正确性、并发安全性与鲁棒性。代理层为纯透传网关，测试重点是路由正确性与故障隔离。

**范围**：`app/` 分层结构下 `core/`（registry/dispatcher/stats）、`api/`（routes/passthrough，含透传端点表）与 `main.py` 装配。
**不在范围**：IP 拉取/测试/租赁逻辑（一级池、二级池）。

## 2. 测试环境与工具

| 项 | 说明 |
|---|---|
| Python | 3.14 |
| 测试框架 | pytest + pytest-asyncio |
| 网络桩 | aioresponses + 本地 mock 二级池服务 |
| 覆盖率 | pytest-cov |
| 依赖文件 | `requirements-dev.txt`：`pytest, pytest-asyncio, pytest-cov, aioresponses` |

## 3. 测试层级

| 层级 | 说明 |
|---|---|
| 单元测试 | registry/dispatcher 纯逻辑，网络打桩 |
| 集成测试 | mock 二级池 → 用户经代理访问闭环 |
| 端到端 | 代理层 + 二级池 + 一级池 + mock 供应商全链路 |
| 性能测试 | 高并发透传 |
| 鲁棒性测试 | 重载失败、单站点故障、上游超时 |

## 4. 测试用例

### 4.1 core/registry.py（路由表）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| PX-REG-001 | 本地文件加载 | 合法 yaml | `load()` | `get/sites` 正确 | P0 |
| PX-REG-002 | 远程 URL 加载 | 桩返回 yaml | `load()` | 同上 | P0 |
| PX-REG-003 | 非法配置（格式错） | 畸形 yaml | `load()` | 抛明确异常 | P0 |
| PX-REG-004 | 非法配置（缺 base_url） | 缺字段 | `load()` | 抛校验异常 | P1 |
| PX-REG-005 | 重载原子替换 | 旧表 + 新表 | 连续 load | 新表生效 | P0 |
| PX-REG-006 | 重载失败保留旧表 | 新配置非法 | 二次 load | 保留旧表，服务可用 | P0 |
| PX-REG-007 | `get` 未配置站点 | 无此站 | get | 返回 None | P1 |
| PX-REG-008 | `sites()` 列表 | 多站点 | 查询 | 返回全部 SiteRoute | P1 |
| PX-REG-009 | `reload_loop` 周期 60s | 假时钟 | 运行 | 间隔 ≈ reload_interval | P1 |

### 4.2 core/dispatcher.py（透传分发）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| PX-DIS-001 | 路径剥离转发 | 配置站点 | `forward(site,"/api/v1/ips/acquire")` | 转发到 `{base_url}/api/v1/ips/acquire` | P0 |
| PX-DIS-002 | method 透传 | GET/POST/DELETE | 各 method | method 保持不变 | P0 |
| PX-DIS-003 | query/body 透传 | 带参数 | 转发 | 参数完整透传 | P0 |
| PX-DIS-004 | 响应原样返回 | 桩返回 `{code,msg,data}` | 转发 | 不解析，原样返回 | P0 |
| PX-DIS-005 | 站点未配置 | 无此站 | forward | 返回明确错误码（非 500） | P0 |
| PX-DIS-006 | 上游不可达 | 桩 ConnectionError | forward | 返回网关错误码 | P0 |
| PX-DIS-007 | 上游超时 | 桩 TimeoutError | forward | 返回网关错误码，不挂起 | P0 |
| PX-DIS-008 | 单站点故障不波及其它 | 两站，一站坏 | 分别 forward | 好站正常，坏站报错 | P0 |

### 4.3 api/（透传端点表 + /health）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| PX-RT-001 | `/health` | 多站点 + 桩一级池 | GET | 返回健康状态 + 站点列表 + 实时聚合 `pools`（一级池 + 各站点二级池 `/status`） | P0 |
| PX-RT-001b | `/health` 展示启动时间与调用统计 | 多次请求 | GET | `started_at`/`uptime`/`stats`（total_calls/calls_by_ip/calls_by_site/errors）正确 | P0 |
| PX-RT-001c | `/health` 下游不可达隔离 | 一级池/某站点不可达 | GET | 对应项 `status={"error":...}`，整体仍 code=0 | P0 |
| PX-RT-002 | `/{site}/status` 透传 | 桩二级池 | GET | 返回二级池 status | P0 |
| PX-RT-003 | `/{site}/count` 透传 | 桩二级池 | GET | 返回二级池 count | P0 |
| PX-RT-004 | `/{site}/ips` 透传 | 桩二级池 | GET | 返回二级池 ips | P0 |
| PX-RT-005 | `/{site}/ips/acquire` 透传 | 桩二级池 | POST | 返回二级池 acquire 结果 | P0 |
| PX-RT-005b | `/{site}/ips/acquire-batch` 透传 | 桩二级池 | POST（带 query） | 返回二级池 acquire-batch 结果；`count`/`strategy` 等查询参数原样转发 | P0 |
| PX-RT-006 | `/{site}/ips/{id}/release` 透传 | 桩二级池 | POST | 返回二级池 release 结果 | P0 |
| PX-RT-007 | `/{site}/ips/{id}` 删除透传 | 桩二级池 | DELETE | 返回二级池删除结果 | P0 |
| PX-RT-008 | `/{site}/ips/release-all` 透传 | 桩二级池 | POST | 返回二级池结果 | P0 |
| PX-RT-009 | 未配置站点请求 | 无此站 | GET 任意 | 返回 40400 站点未配置 | P0 |
| PX-RT-010 | 上游业务码原样透传且不计入自身 errors | 桩返回 `code:40402` | POST acquire | body 原样 `40402`，`stats.errors` 不含 40402 | P0 |
| PX-RT-011 | 业务码访问日志 | 请求触发 | caplog | 日志含 `http=... biz=<code> method=... path=...` | P1 |
| PX-RT-012 | 上游不可达/超时 50200 | 桩异常/超时 | 透传 | 返回 `{code:50200}`，errors 计数 | P0 |
| PX-RT-013 | 单站点故障不波及其它 | 一好一坏 | 分别请求 | 好站正常、坏站 50200 | P0 |

### 4.4 core/stats.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| PX-STAT-001 | `record_call` 计数 | 构造 | 多次调用 | total_calls 与 calls_by_ip 正确 | P0 |
| PX-STAT-002 | `record_site`/`record_error` | 构造 | 调用 | calls_by_site/errors 正确 | P0 |
| PX-STAT-003 | `snapshot` 返回副本 | 构造 | 调用后修改 | 快照不受外部修改影响 | P1 |
| PX-STAT-004 | `start_time` 默认当前时间 | 构造 | 读取 | 与传入值/当前时间一致 | P1 |

## 5. 集成测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| PX-INT-001 | mock 二级池 → 用户经代理 acquire | 路由正确、透传成功 | P0 |
| PX-INT-002 | 改配置 60s 后热更新生效 | 新增站点无需重启即可访问 | P0 |
| PX-INT-003 | 多站点并发访问互不串扰 | 各站点路由到正确二级池 | P0 |

## 6. 鲁棒性与性能测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| PX-ROB-001 | 重载失败 | 服务不中断，旧表继续用 | P0 |
| PX-ROB-002 | 单站点故障 | 仅该站点报错 | P0 |
| PX-PERF-001 | 高并发透传 | 转发不串扰、吞吐稳定 | P1 |

## 7. 测试桩与 Fixture 设计

- mock 二级池：本地 FastAPI 服务，实现 status/count/ips/acquire/release/delete/release-all，返回固定 `{code,msg,data}`，可配置故障/延迟。
- `conftest.py`：
  - `event_loop`、`mock_session`（aioresponses）
  - `registry`：预置多站点路由表的 Registry fixture
  - `dispatcher`：指向 mock 二级池的 Dispatcher fixture
  - `tmp_routes`：临时路由配置文件 fixture
- 桩对象：二级池正常响应 / ConnectionError / TimeoutError / 非法配置。

## 8. 覆盖率目标

| 模块 | 目标 |
|---|---|
| registry / dispatcher / routes / stats | ≥ 95% |
| 整体 | ≥ 90% |

## 9. 执行方式

```bash
pip install -r requirements-dev.txt

# 单元 + 集成
pytest -v --cov=app --cov-report=term-missing

# 仅 registry
pytest tests/test_registry.py -v

# 仅 dispatcher
pytest tests/test_dispatcher.py -v

# 集成
pytest tests/test_integration.py -v

# 性能
pytest tests/test_perf.py -v -m perf
```

## 10. 缺陷管理与退出准则

- **缺陷分级**：P0（阻断）必须修复复测；P1（重要）需修复；P2 可排期。
- **退出准则**：P0/P1 清零；registry/dispatcher/routes/stats 覆盖率 ≥ 95%；全部用例通过后方可发布。
- 代理层改动需回归与二级池的透传契约（路径剥离、`{code,msg,data}` 原样返回）。
