# 公共库（ip_pool_common）测试计划书

## 1. 测试目标与范围

验证公共库各模块（数据模型、代理测试原语、配置加载、日志、API 通用件）的**正确性、稳定性与通用性**。公共库被三个业务项目依赖，其正确性是全局正确性的前提，测试标准从严。

**范围**：`ip_pool_common/` 下 `models.py`、`testing/`（reachability/site/batch）、`errors.py`、`concurrency.py`、`config.py`、`logging_setup.py`、`api/`（codes/responses/middleware/runner）全部公开接口。
**不在范围**：三个业务项目的业务逻辑（见各自测试计划书）。

## 2. 测试环境与工具

| 项 | 说明 |
|---|---|
| Python | 3.14（项目 `.venv`） |
| 测试框架 | pytest + pytest-asyncio |
| 网络桩 | 本地 HTTP CONNECT 代理桩 + 本地 SOCKS4/5 桩服务器（真实 socket 握手）；aioresponses（模拟 `site_test` 的目标站点 HTTP 响应） |
| 覆盖率 | pytest-cov |
| 依赖文件 | `requirements-dev.txt`：`pytest, pytest-asyncio, pytest-cov, aioresponses` |

## 3. 测试层级

| 层级 | 说明 |
|---|---|
| 单元测试 | 各模块纯逻辑，全部网络交互打桩，不依赖真实外网 |

## 4. 测试用例

### 4.1 models.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| C-MOD-001 | `build_proxy_url` 组装四种协议 | 无 | 分别传 http/https/socks4/socks5 | 返回 `http://ip:port` / `https://ip:port` / `socks4://ip:port` / `socks5://ip:port` | P0 |
| C-MOD-002 | `Protocol` 枚举取值 | 无 | 遍历合法值，构造非法值 | 仅 4 个合法值；非法值抛异常 | P0 |
| C-MOD-003 | `IpRecord`/`Level2Record`/`ProviderIp` 默认值 | 无 | 用默认构造 | 字段默认值符合定义 | P1 |
| C-MOD-004 | `ProviderIp.ttl` 可空 | 无 | ttl 传 None | 接受 None，无异常 | P1 |

### 4.2 testing/（reachability / site / batch）与 errors.py

> 说明：`proxy_reachability_test` 已改为**纯 python_socks 握手**（兼容 lazy-CONNECT 代理），代理桩用本地 HTTP CONNECT / SOCKS 服务器提供真实握手字节流，而非 aioresponses 打桩。

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| C-TEST-001 | http 代理可达性：正常握手 | 桩：http 代理返回 200 | 调 `proxy_reachability_test` | `(True, latency>=0)` | P0 |
| C-TEST-002 | http 代理返回 407 仍判可用 | 桩：返回 407 | 同上 | `(True, _)` | P0 |
| C-TEST-002b | http 代理返回 502 合法应答仍判可用 | 桩：返回 502 | 同上 | `(True, _)`（`_is_legal_proxy_reply` 判定 [400,599) 为合法应答） | P0 |
| C-TEST-003 | 代理端口不通/连接拒绝 | 桩：未监听端口 | 同上 | `(False, _)` | P0 |
| C-TEST-004 | 代理握手超时 | 桩：不应答 | 同上 | `(False, _)`，且不挂起 | P0 |
| C-TEST-005 | socks4 握手成功 | 桩：socks4 应答 | 同上 | `(True, _)` | P0 |
| C-TEST-006 | socks5 握手成功 | 桩：socks5 应答 | 同上 | `(True, _)` | P0 |
| C-TEST-007 | socks 握手失败/被拒 | 桩：拒绝应答 | 同上 | 按协议判定，返回 `(False, _)` | P1 |
| C-TEST-008 | 非法 proxy_url | 畸形地址 | 同上 | `(False, _)`，原因 `invalid_proxy` | P1 |
| C-TEST-009 | `site_test` 出口可达 | 桩：目标站 200 | 经代理访问目标 | `(True, latency)` | P0 |
| C-TEST-010 | `site_test` 4xx 判可达、5xx 失败 | 桩：4xx / 5xx | 同上 | 4xx `(True, _)`；5xx `(False, _)` | P0 |
| C-TEST-011 | `site_test` 超时/连接错误 | 桩：超时/断连 | 同上 | `(False, _)` | P0 |
| C-TEST-012 | `batch_test` 仅返回通过项且保序 | 混合通过/失败 | 并发批量测试 | 返回列表仅含通过项且顺序不变 | P0 |
| C-TEST-013 | `batch_test` 并发上限 | 计数信号量 | 大量输入 | 并发不超过 concurrency | P1 |
| C-TEST-014 | `batch_test` 空输入/异常项 | 空列表 / 抛异常 | 调各测试函数 | 返回空/剔除异常项，不抛异常 | P1 |
| C-TEST-015 | `classify_test_error` 归类 | 各类异常 | 归类 | timeout/connect/proxy_reject/invalid_proxy/client_error/exception 判定正确 | P1 |
| C-TEST-016 | `*_detailed` 返回失败原因 | 桩：超时/5xx/断连/非法地址 | 调 detailed 变体 | `(ok, latency, reason)`，reason 正确 | P1 |

### 4.3 config.py（含 deep_merge）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| C-CFG-001 | `load_yaml` 读取合法文件 | 临时 yaml | 读取 | 返回 dict | P0 |
| C-CFG-002 | `load_yaml` 文件不存在 | 不存在路径 | 读取 | 抛明确异常 | P1 |
| C-CFG-003 | `load_settings` yaml + 环境变量覆盖 | 设置 env | 实例化 Settings | 环境变量优先级更高 | P0 |
| C-CFG-004 | `load_settings` 缺必填项 | 缺字段 yaml | 实例化 | 抛明确校验异常 | P0 |
| C-CFG-005 | `load_settings` 默认值填充 | 最小 yaml | 实例化 | 缺省字段用默认值 | P1 |
| C-CFG-006 | `deep_merge` 标量覆盖与嵌套递归合并 | base/override dict | 合并 | 新 dict 返回、入参不变 | P0 |

### 4.4 logging_setup.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| C-LOG-001 | `setup_logging` 冒烟 | 无 | 初始化 | 不抛异常，级别生效 | P1 |
| C-LOG-002 | 文件输出 | 指定 log_file | 初始化+写日志 | 文件生成且含日志内容 | P2 |

### 4.5 api/（codes / responses / middleware / runner）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| C-API-001 | `ok(data)` 封装 | 无 | 调用 | `{"code":0,"msg":"ok","data":data}` | P0 |
| C-API-002 | `ok()` 默认 data 为 None | 无 | 调用 | `{"code":0,"msg":"ok","data":None}` | P1 |
| C-API-002b | `ok()` 附加顶层字段 | 无 | `ok(data, max_id=5)` | `{"code":0,...,"max_id":5}` 不破坏原契约 | P0 |
| C-API-003 | `err(code,msg)` 封装 | 无 | 调用 | `{"code":code,"msg":msg,"data":None}` | P0 |
| C-API-003b | `err` 接受 ErrorCode 枚举 | 无 | 传枚举 | code 转 int，输出正确 | P1 |
| C-API-004 | `ErrorCode` 枚举值唯一且符合约定 | 无 | 遍历 | OK=0/PARAM_ERROR=40000/NOT_FOUND=40400/EMPTY_POOL=40402/INTERNAL=50000/UPSTREAM_ERROR=50200 | P1 |
| C-API-005 | `ApiCounterMiddleware` 计数 | 构造中间件 | 多次请求 | 每请求 +1，`count` 正确 | P0 |
| C-API-006 | `ApiCounterMiddleware` 忽略非 http scope | 构造 | lifespan 消息 | 不计数 | P1 |
| C-API-007 | `BizCodeLogMiddleware` 输出业务码日志 | caplog | 请求返回 JSON | 日志含 `http=... biz=<code> method=... path=...` | P0 |
| C-API-008 | `BizCodeLogMiddleware` 非 JSON body | caplog | 返回非 JSON | 业务码记 `-` | P1 |
| C-API-009 | `BizCodeLogMiddleware` 透传不改响应 | 构造 | 请求 | 响应体/状态码不变 | P1 |
| C-API-010 | `run_app` 启动 | 最小 FastAPI app | 启动并请求 /health | 正常响应后关闭 | P1 |

## 5. 测试桩与 Fixture 设计

- `conftest.py`：
  - `event_loop`：session 级事件循环
  - `aio_mock` / `mock_session`：基于 `aioresponses` 的 aiohttp 桩（控制 `site_test` 的目标站点 HTTP 响应）
  - `http_proxy_server`：本地 HTTP CONNECT 代理桩（mode ∈ ok/auth/refuse/timeout/reset/refused），返回 `async (mode) -> proxy_url`
  - `socks_server`：本地 SOCKS4/5 桩服务器（mode ∈ normal/refuse/timeout），返回 `async (version, mode) -> url`
  - `tmp_yaml`：临时配置文件 fixture
- 桩对象：
  - 正常 http 代理（200）、鉴权代理（407）、合法拒绝（502）、连接拒绝（未监听端口）、超时（不应答）、断连（reset）
  - socks4/socks5 正常/拒绝/超时应答字节流

## 6. 覆盖率目标

| 模块 | 目标 |
|---|---|
| models / api / config | 100% |
| testing | ≥ 95% |
| logging_setup | ≥ 80% |
| 整体 | ≥ 90% |

## 7. 执行方式

```bash
# 安装依赖
pip install -r requirements-dev.txt
pip install -e ../common

# 运行全部测试 + 覆盖率
pytest -v --cov=ip_pool_common --cov-report=term-missing

# 仅运行某模块
pytest tests/test_testing.py -v
```

## 8. 缺陷管理与退出准则

- **缺陷分级**：P0（阻断，影响三项目）必须修复并复测通过；P1（重要）需修复；P2 可排期。
- **退出准则**：P0/P1 缺陷清零；整体覆盖率 ≥ 90%；全部用例通过后方可发布公共库新版本。
- 公共库改动需在三个业务项目上跑通各自测试（回归）后方可合入。
